// file2.cpp

#include "myheader.h"
#include <iostream>
#include <stdio.h>
#include <string.h>

void printGame(struct Game* question)
{
  std::cout << "printing from a different cpp file!";
}

printf("\n");

  strcpy(firstname);
 
  strcpy(lastname);

  for (int i = 0; i < 2; i++)
         {
        printf("Player Names:  %d:\n", i+1);
        printf("\n");
         }

}
 
void printGame(struct Game* question)
{
  printf(" First name: %s\n", student->firstname);
  printf(" Last name: %s\n", student->lastname);
}

